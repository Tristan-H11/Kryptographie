/**
 * Darstellung der Clients.
 * Dies ist die Sammlung an möglichen Kommunikationspartnern.
 * Soll ein neuer Client hinzugefügt werden, muss dieser hier eingetragen werden.
 */
export enum ClientEnum {
  Alice = 'Alice',
  Bob = 'Bob'
}
